version: '3.8'

services:
  app:
    build: .
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - stack.env
    volumes:
      - uploads:/app/public/uploads

volumes:
  uploads:
    driver: local 